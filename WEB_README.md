# 超星学习通自动化工具 - Web版本

## 🌐 概述

这是超星学习通自动化工具的Web版本，基于Streamlit构建，提供友好的Web界面来配置和执行自动化学习任务。

## ✨ 主要特性

- 🎨 **现代化Web界面** - 直观的用户界面，无需命令行操作
- ⚙️ **可视化配置** - 通过表单轻松配置所有参数
- 📊 **实时进度监控** - 实时显示任务执行状态和进度
- 📋 **详细日志显示** - 分类显示执行日志，支持调试模式
- 🔄 **任务控制** - 支持开始、暂停、重置任务执行
- 💾 **配置验证** - 自动验证配置参数的有效性

## 🚀 快速开始

### 1. 安装依赖

```bash
pip install -r requirements.txt
```

### 2. 启动Web应用

#### 方法一：使用启动脚本
```bash
python run_web.py
```

#### 方法二：直接使用Streamlit
```bash
streamlit run streamlit_app.py
```

### 3. 访问应用

启动后，浏览器会自动打开 `http://localhost:8501`

## 📖 使用说明

### 配置设置

在左侧边栏中配置以下参数：

#### 🔐 登录配置
- **使用Cookies登录**：勾选此项可使用已保存的cookies登录，无需输入账号密码
- **手机号**：登录账号的手机号
- **密码**：登录密码

#### 📚 课程配置
- **课程ID列表**：要学习的课程ID，多个ID用逗号分隔（如：123456,789012）
  - 留空则学习所有课程
  - 可以在课程获取功能中查看可用课程ID

#### 🎯 学习配置
- **视频播放倍速**：1.0-2.0倍速度播放
- **并发章节数**：同时处理的章节数量（1-10）
- **未开放任务处理方式**：
  - `retry`：重试（默认）
  - `ask`：询问用户
  - `continue`：跳过继续
- **启用调试模式**：显示详细的调试日志

#### 📖 题库配置
- **启用题库功能**：是否使用题库自动答题
- **题库提供商**：选择题库服务
- **查询延迟**：每次查询的间隔时间（秒）
- **答案覆盖率**：自动答题的覆盖率（0-1）
- **自动提交答案**：是否自动提交答案

#### 🔔 通知配置
- **启用通知功能**：任务完成后发送通知
- **通知服务**：选择通知服务提供商
- **通知URL**：配置通知服务的URL或Token

### 任务执行

1. **配置验证**：点击"🔍 验证配置"按钮检查参数是否正确
2. **开始执行**：点击"🚀 开始执行"按钮开始自动化学习
3. **监控进度**：在主界面查看执行状态、进度和日志
4. **控制任务**：可以随时暂停、重置任务执行

### 日志说明

- 🔴 **ERROR**：错误信息，需要关注
- 🟡 **WARNING**：警告信息，可能影响执行
- 🔵 **INFO**：一般信息，正常执行流程
- ⚪ **DEBUG**：调试信息，仅在启用调试模式时显示

## 🛠️ 技术架构

### 核心模块

- **`streamlit_app.py`** - Streamlit主应用
- **`api/web_config.py`** - Web配置管理
- **`api/web_adapter.py`** - Web适配器，桥接UI和核心API
- **`api/base.py`** - 超星API核心功能
- **`api/answer.py`** - 题库功能
- **`api/notification.py`** - 通知功能

### 配置流程

1. 用户在Web界面设置参数
2. 配置保存到Streamlit session_state
3. WebConfigManager验证和管理配置
4. WebChaoxingAdapter使用配置执行任务
5. 实时更新界面状态和日志

## 🔧 故障排除

### 常见问题

1. **无法启动应用**
   - 检查Python版本（需要3.8+）
   - 确保所有依赖已安装：`pip install -r requirements.txt`

2. **配置验证失败**
   - 检查必填字段是否完整
   - 确认数值范围是否正确
   - 查看具体的错误提示

3. **登录失败**
   - 检查账号密码是否正确
   - 尝试使用cookies登录
   - 检查网络连接

4. **任务执行失败**
   - 查看详细错误日志
   - 检查课程ID是否正确
   - 确认课程是否已开放

### 调试模式

启用"启用调试模式"可以看到更详细的执行日志，帮助诊断问题。

## 📝 注意事项

- 本工具仅用于学习讨论目的
- 请妥善保管账号密码信息
- 建议在非高峰时段使用，避免给服务器造成压力
- 遵守相关法律法规和平台使用条款

## 🆚 与命令行版本的区别

| 功能 | 命令行版本 | Web版本 |
|------|-----------|---------|
| 配置方式 | INI文件/命令行参数 | Web表单 |
| 界面 | 命令行 | 图形界面 |
| 实时监控 | 文本输出 | 可视化进度 |
| 日志查看 | 终端输出 | 分类显示 |
| 任务控制 | Ctrl+C中断 | 界面按钮控制 |
| 配置验证 | 启动时检查 | 实时验证 |

## 🤝 贡献

欢迎提交Issue和Pull Request来改进这个Web版本！

## 📄 许可证

本项目遵循GPL-3.0开源协议。